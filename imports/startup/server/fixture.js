export default ({ Collections }) => {
  // const { Users } = Collections;
  //
  // let adminId;
  // const adminEmail = 'admin@example.com';
  // if (!Users.findOne({ 'emails.0.address': adminEmail, roles: { $elemMatch: { $eq: 'owner' } } })) {
  //   adminId = Accounts.createUser({
  //     email: adminEmail,
  //     password: 'admin2017',
  //     profile: {
  //
  //     }
  //   });
  //   Users.update(adminId, { $set: { roles: ['owner', 'admin'] } });
  // }
};
